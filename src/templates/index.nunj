{% set title = demo.title ~ ' ' ~ version %}
{% set bodyClass = 'home' %}

{% extends 'layouts/default.nunj' %}

{% block body %}

	<h1>{{ demo.title }} {{ version }}</h1>

	<p>{{ demo.description }}</p>

{% endblock %}
